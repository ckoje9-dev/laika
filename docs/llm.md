# LLM / RAG

- Index: project metadata, extracted building summary, drawing-level stats, semantic object summaries.
- Exclude: raw coordinates, full DXF text.
- QA: retrieve structured records + snippets, generate answer, include uncertainty when data is provisional.
- Prompts: keep tool/system prompts versioned; log citations to entities/objects.
