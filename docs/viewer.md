# Viewer

- Input: DXF; render via SVG or Canvas.
- Controls: layer on/off, zoom/pan, fit-to-view.
- UX: hover shows metadata (layer, type, bbox, length/area).
- Future: selection sync to semantic objects and Q&A highlights.
